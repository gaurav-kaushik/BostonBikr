from flask import Flask
app = Flask(__name__)
from app import views

"""
This script should be in your 'app' folder, where you application package is located. We create the object 'Flask' as app. Then we invoke the views.py file. 
"""
